# Reglas del Proyecto LMS Alquimia

## Control de Versiones (Git)
- **REGLA MANDATORIA**: NUNCA ejecutar `git push` automáticamente. Siempre mostrar el resumen de cambios/resultado al usuario y pedir confirmación explícita antes de realizar un `git push`.
